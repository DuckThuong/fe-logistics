import { ORDER_PAGE_SECTIONS } from "../../data/content";
import { ServiceDetailPage } from "../../components/ServiceDetailPage";

export const OrderPage = () => (
  <ServiceDetailPage title="Đặt hàng Trung Quốc" sections={ORDER_PAGE_SECTIONS} />
);
